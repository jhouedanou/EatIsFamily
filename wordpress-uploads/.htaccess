# Allow SVG and SVGZ files to be served with correct MIME types
<FilesMatch "\.svgz?$">
    AddType image/svg+xml svg svgz
    AddEncoding gzip svgz
</FilesMatch>

# Allow direct access to media files
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>
